name: Android CI

on:
  workflow_dispatch:
  
  push:
    branches: [ main, master, develop ]
    paths:
      - 'app/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
      - 'gradle/**'
      - 'gradlew'
      - 'gradlew.bat'
      - 'gradle.properties'
      - '.github/workflows/android-ci.yml'
  pull_request:
    branches: [ main, master, develop ]
    paths:
      - 'app/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
      - 'gradle/**'
      - 'gradlew'
      - 'gradlew.bat'
      - 'gradle.properties'
      - '.github/workflows/android-ci.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

    - name: Set up debug keystore from secret
      env:
        ANDROID_DEBUG_KEYSTORE_BASE64: ${{ secrets.ANDROID_DEBUG_KEYSTORE_BASE64 }}
      run: |
        if [ -n "$ANDROID_DEBUG_KEYSTORE_BASE64" ]; then
          mkdir -p ~/.android
          echo "$ANDROID_DEBUG_KEYSTORE_BASE64" | base64 --decode > ~/.android/debug.keystore
          echo "✅ Custom debug.keystore installed from secret"
        else
          echo "ℹ️ ANDROID_DEBUG_KEYSTORE_BASE64 not set – using default runner debug keystore"
        fi

    - name: Verify debug keystore (SHA-256)
      run: |
        if [ -f ~/.android/debug.keystore ]; then
          keytool -list -v -keystore ~/.android/debug.keystore \
            -alias androiddebugkey -storepass android -keypass android \
            | grep -E "SHA256|Certificate fingerprint"
        else
          echo "ℹ️ No custom debug.keystore found, skipping verification"
        fi
      
    - name: Set up JDK 17
      uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
        
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Run Unit Tests
      run: ./gradlew testDebugUnitTest --info --stacktrace --scan
      continue-on-error: true
    - name: Upload Test Reports
      if: always()
      uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
      with:
        name: test-reports
        path: app/build/reports/tests/testDebugUnitTest/  
    - name: Build Debug APK
      run: ./gradlew assembleDebug --stacktrace
      
    - name: Upload Debug APK
      uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
      with:
        name: app-debug
        path: app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30
        
    - name: Verify APK signing certificate
      run: |
        if [ -f ~/.android/debug.keystore ]; then
          apksigner verify --print-certs -v \
            app/build/outputs/apk/debug/app-debug.apk \
            | grep -E "SHA-256|Signer"
        else
          echo "ℹ️ No custom debug.keystore found, skipping APK cert verification"
        fi

    - name: Build info
      run: |
        echo "✅ Build completed successfully!"
        echo "📱 APK Location: app/build/outputs/apk/debug/app-debug.apk"
        ls -lh app/build/outputs/apk/debug/app-debug.apk || echo "APK not found"
